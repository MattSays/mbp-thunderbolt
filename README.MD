# mbp-thunderbolt

This is a workaround that makes you boot on any MacBook Pro, any Linux kernel 5.11+ on any usb3 drives.

## Requirements

The only requirement is to have `initcpio` installed.

## Installation

To install, just type 
```
./install.sh
```